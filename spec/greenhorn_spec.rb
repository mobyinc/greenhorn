require 'spec_helper'

describe Greenhorn do
  it 'has a version number' do
    expect(Greenhorn::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
